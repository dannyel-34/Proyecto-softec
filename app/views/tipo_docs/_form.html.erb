
<%= simple_form_for(@tipo_doc, :html => { :class => 'form-horizontal', remote: true } ) do |f| %>

  <% if @tipo_doc.errors.any? %>
	<div id="error_explanation">
  	<h2><%= pluralize(@tipo_doc.errors.count, "error") %> prohibited this municipio from being saved:</h2>

  	<ul>
  	<% @tipo_doc.errors.full_messages.each do |message| %>
    	<li><%= message %></li>
  	<% end %>
  	</ul>
	</div>
  <% end %>

  <div class="form-group">
   	<label for="name" class="col-sm-3 control-label">Nombre</label>
   	<div class="col-md-4">
      	<%= f.input_field :nombre, class: 'form-control' %>
   	</div>
  </div>
  
  
  <div class="form-group">
   	<label for="name" class="col-sm-3 control-label">Sigla</label>
   	<div class="col-md-4">
      	<%= f.input_field :sigla, class: 'form-control' %>
   	</div>
  </div>

  <div class="form-group">
	<div class="col-sm-offset-2 col-sm-10">
  	<%= f.submit "Guardar", class: "btn btn-info" %>
	</div>
  </div>

<% end %>

